# 기능 
북마크 관리 Chrome Extention의 AppHeader 영역에 배치되어, 전체 collections(보관중인 링크 묶음)의 내용을 검색하여 표시하거나 새로운 collection을 추가하는 기능을 담당하는 인터페이스. 

# Pseudo Code

Interface BoxSizingSystem(
    Properties(
        size: float,
        areaFor: str
    ),
    Functions(
        swap{
                구현: size와 areaFor를 유지하고 `Blank <-> TextInputBox` 변환. (사이즈가 1이 아닌 Blank는 TextInputBox로 변환될 수 없음)
            },
    )
)

Class Blank {implements: BoxSizingSystem}(
    Properties(
        size: float,
        areaFor: str,
    )
)

Class TextInputBox {implements: BoxSizingSystem}(
    Properties(
        size: 1,    // TextInputBox는 항상 1(최대비율) 사이즈임.
        areaFor: str
    )
)

Class ToolbarOrder (
    Properties(
        placement: list,
        state: str,
    )
)

appHeaderArea = `앱 헤더 영역 (바깥 클릭 확인하기 위해)`    // Toolbar ⊃ AppHeader ⊃ 전체요소(bottom-sheet, collections 포함)인데, Toolbar를 제외한 AppHeader 영역을 클릭했을 때의 action을 정의하기 위함.

search_button = IconButton("material-symbols: search icon")
add_button = IconButton("material-symbols: plus icon")
tb_order = ToolbarOrder(
    placement: [Blank(size: 1, areaFor: "default"), search_button, Blank(size: 0, areaFor: "search"), add_button, Blank(size: 0, areaFor: "add")]  // default
    state: "default"
)
toolbar = dynamic HorizontalGrid(tb_order)

if `버튼이 클릭되면` :
    if last_click = "search" :
        Animate(tb_order, last_click)
        sleep(0.1 seconds)
        구현: `~.areaFor==last_click`인 Blank를 TextInputBox로 swap
        // 이 상태가 되어 있어야 함 : placement = [Blank(size: 0), search_button, Blank(size: 1), add_button, Blank(size:0)]
        placement.swap_blank_to_text_input_box(which_is size:1)

    if last_click = "add" :
        Animate(tb_order, last_click)
        sleep(0.1 seconds)
        구현: `~.areaFor==last_click`인 Blank를 TextInputBox로 swap
        // 이 상태가 되어 있어야 함 : placement = [Blank(size: 0), search_button, Blank(size: 0), add_button, Blank(size: 1)]
        placement.swap_blank_to_text_input_box(which_is size:1)

    if last_click not in ["search", "add"] and last_click in appHeaderArea :
        Animate(tb_order, "default")
        // 이 상태가 되어 있어야 함 : placement = [Blank(size: 1), search_button, Blank(size: 0), add_button, Blank(size: 0)]    // back to default.
        // no text input box for default


## Animation Details

function Animate(tb_order){
    if TextInputBox in tb_order: 
        TextInputBox.swap
    구현: placement에서 `~.areaFor == last_click`인 Blank 요소의 size가 1(최대비율)이 되고 나머지 Blank들은 size가 0(최소비율)으로 축소되도록 animate.
}


## Output 양식
{
    toolbar_operation: "search" or "add",
    toolbar_input: str
}